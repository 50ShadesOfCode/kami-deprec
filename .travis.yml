language: rust

rust:
  - stable
  - beta
  - nightly

jobs:
  allow_failures:
    - rust: nightly
    fast_finish: true

branches:
  only:
    # release tags
    - /^v\d+\.\d+\.\d+.*$/
    - master

cache: cargo